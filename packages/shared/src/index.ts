/**
 * @fileoverview Shared types and constants for the Hanafuda monorepo.
 *
 * This package exports types and constants used by both the frontend
 * (Nuxt SPA) and backend (Hono server) packages.
 */

// Types will be extracted from the frontend in a follow-up.
// For now, this is a placeholder to verify the workspace link works.
export const PACKAGE_NAME = '@hanafuda/shared' as const
